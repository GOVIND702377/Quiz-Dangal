# Copy this file to .env.local and fill values. .gitignore prevents committing .env* files.

# Public (used by frontend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
## Set to 1 to skip Supabase auth locally (UI only testing)
VITE_BYPASS_AUTH=0

# Server-only (DO NOT put in frontend, never commit)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=